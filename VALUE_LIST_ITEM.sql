--------------------------------------------------------
--  DDL for Type VALUE_LIST_ITEM
--------------------------------------------------------

  CREATE OR REPLACE TYPE "VALUE_LIST_ITEM" AS OBJECT (
   DISPLAY VARCHAR2(255),
   VALUE NUMBER (12,0)
);

/
